# Definition: ssh establishes secure remote shell sessions.
# Use it for: connecting to servers or running remote commands.

# SSH into server
```
ssh user@server
```
# Execute remote command
```
ssh user@server "ls -al /var/log"
```
